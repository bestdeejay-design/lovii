# lovii — ПРОД (https://lovii.ru)

## ЖЕЛЕЗНОЕ ПРАВИЛО

- Этот репо — **ПРОД**. Прямые правки и пуши в `main` **ЗАПРЕЩЕНЫ**.
- Контент правится в `bestdeejay-design/lovii-site` (стенд).
- На прод попадает **только через CI**: Actions в `lovii-site` →
  `publish-prod` → Run workflow → confirm=`PUBLISH`, и только по явной
  команде владельца «публикуем на прод».
- Исключение: файлы самого правила (`AGENTS.md`, CI прода) — только по явной
  просьбе владельца, контент сайта при этом не трогать.
- История прода: ветки `archive/*`, теги `prod-*` (первый слепок:
  `archive/prod-2026-09-24`). Не удалять.
